# Contributing

Issues and pull requests are welcome.

Before opening a pull request:

1. Keep secrets out of the repository.
2. Run the app locally with `python app.py`.
3. Update `README.md` when behavior or setup steps change.
4. Keep generated files such as `data/`, `build/`, and `dist/` out of commits.
